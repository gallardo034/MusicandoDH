<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editar cancion</title>
    <style>
        body {
            background-color: darkcyan;
            color: white;
        }

        h1 {
            text-align: center;
            text-decoration: overline;
        }

        ul{
            text-align: center;
            margin-top: 10px;
        }

        a {
            color: white;
            text-align: center;
            text-decoration: none;
            font-size: 18px;
        }

        li{
            list-style: none;
            margin: 10px;
        }

        div {
            color: black;
            text-align: center;
            font-size: xx-large;
            text-decoration: double;
        }
        
        input{
            display: inline-block;
            margin: 5px;
            text-align: center;
            align-items: center;
        }

        form{
            display: flex;
            align-content: center;
            align-items: center;
            align-self: center;
            justify-content: center;
            flex-direction: column;
        }

        input{
            margin-top: 15px;
            font-size:large;
            border-radius: 5px;
            border: 0px;
        }

    </style>
</head>
<body>
    <h1>Editar Cancion</h1>

    <form action="/canciones/<%= cancion.id %>/editar?_method=PUT" method="POST">
    <label for="id">ID</label>
    <input type="number" name="id"  value= "<%= cancion.id %>" placeholder="id">
    <br>
    <label for="titulo">TITULO</label>
    <input type="text" name="titulo" value= "<%= cancion.titulo %>" placeholder="titulo">
    <br>
    <label for="duracion">DURACION</label>
    <input type="number" name="duracion" value= "<%= cancion.duracion %>" placeholder="duracion">
    <br>
    <label for="genero_id">GENERO ID</label>
    <input type="number" name="genero_id" value= "<%= cancion.genero_id %>" placeholder="genero_id">
    <br>
    <label for="album_id">ALBUM ID</label>
    <input type="number" name="album_id" value= "<%= cancion.album_id %>" placeholder="album_id">
    <br>
    <label for="artista_id">ARTISTA ID</label>
    <input type="number" name="artista_id" value= "<%= cancion.artista_id %>" placeholder="artista_id">
    <input type="submit" value="editar">
    </form>
</body>
</html>